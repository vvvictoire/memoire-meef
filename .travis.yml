sudo: require
language: bash

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y texlive-xetex texlive-latex-recommended texlive-latex-extra texlive-base texlive-latex-base

script:
      - bash build.sh
